#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "rectangleSolver.h"

// Katarina added the c file for the rectangleSolver.c

int analyzeRectangle(int pair1, int pair2, int pair3, int pair4) {

	// takes the pairs of points and forms 4 lines
	// you can form lines from two points using the distance formula 
}

int isItaRectangle(int pair1, int pair2, int pair3, int pair4) {

	// takes those 4 lines and compares to see if 2 of them are the same length, if they are the same length, it will report the area of the rectangle and output the perimeter 
}